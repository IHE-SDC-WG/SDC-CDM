# Old OMOP Extension Model

This folder contains archived artifacts from the previous model that placed SDC tables and SDC fields inside the OMOP database shape.

These files are kept for history only:

- `database/postgresql-sdc-ddl/`
- `database/sqlite-sdc-ddl/`
- `database/ddl-old/`
- `OMOP_SDC_MVP_README.md`
- `diagrams/sdc-sdm-modifications/`
- `notebooks/test_omop_sdc_mvp.dib`

Active schema work should use `database/SCHEMA_ARCHITECTURE.md`, `database/schemas/`, and `database/etl/`.
